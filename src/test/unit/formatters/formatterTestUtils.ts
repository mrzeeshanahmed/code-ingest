export type { FormatterDigestOverrides as DigestFactoryOverrides } from "../../support/formatters.fixture";
export { createFormatterDigestFixture as createDigestResult } from "../../support/formatters.fixture";