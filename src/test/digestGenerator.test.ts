import { describe, expect, it } from "@jest/globals";

describe.skip("DigestGenerator legacy suite", () => {
  it("is pending refactor to align with modern pipeline", () => {
    expect(true).toBe(true);
  });
});