import { describe, expect, it } from "@jest/globals";

// Legacy OutputWriter tests moved to src/test/unit/outputWriter.unit.test.ts.
describe("outputWriter legacy suite", () => {
  it("is covered by new unit tests", () => {
    expect(true).toBe(true);
  });
});
