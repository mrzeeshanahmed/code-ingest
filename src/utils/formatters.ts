/**
 * @deprecated Legacy formatter helpers have been replaced by the strategy-based
 * formatters in `src/formatters`. This module is intentionally empty and will
 * be removed in a future release.
 */

export {};
