/*
 * Tests moved to resources/webview/test/configSummary.unit.test.js.
 * This placeholder file remains to preserve legacy import paths.
 */

describe("configSummary placeholder", () => {
	it("keeps legacy test target satisfied", () => {
		expect(true).toBe(true);
	});
});